import { ResposiveImageDirective } from './resposive-image.directive';

describe('ResposiveImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ResposiveImageDirective();
    expect(directive).toBeTruthy();
  });
});
