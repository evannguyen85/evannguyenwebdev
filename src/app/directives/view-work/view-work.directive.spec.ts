import { ViewWorkDirective } from './view-work.directive';

describe('ViewWorkDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewWorkDirective();
    expect(directive).toBeTruthy();
  });
});
